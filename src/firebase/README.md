# Módulo de Configuración Firebase (`src/firebase`)

## Descripción
Contiene la inicialización de servicios de Firebase (Authentication y Firestore) utilizando las credenciales provistas en `firebase-applet-config.json`.

## Archivos
- `config.ts`: Inicializa Firebase App, Auth y Firestore.
